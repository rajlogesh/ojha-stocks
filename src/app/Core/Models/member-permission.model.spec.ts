import { MemberPermission } from './member-permission.model';

describe('MemberPermission', () => {
  it('should create an instance', () => {
    //expect(new MemberPermission()).toBeTruthy();
  });
});
